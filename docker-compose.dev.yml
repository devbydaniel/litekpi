services:
  db:
    image: postgres:15-alpine
    container_name: trackable-db-dev
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: trackable
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: trackable
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trackable -d trackable"]
      interval: 5s
      timeout: 5s
      retries: 5

  mailcatcher:
    image: schickling/mailcatcher
    container_name: trackable-mail-dev
    ports:
      - "1025:1025"   # SMTP
      - "1080:1080"   # Web UI

volumes:
  postgres_data_dev:

networks:
  default:
    name: trackable-dev-network
